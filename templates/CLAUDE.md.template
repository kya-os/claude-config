# [directory-name]/

One sentence describing this directory's purpose.

## Files

| File | What | When to read |
|------|------|--------------|
| `example.ts` | Does X | When working on X |
| `other.ts` | Manages Y | When Y behavior unclear |

## Subdirectories

| Directory | What | When to read |
|-----------|------|--------------|
| `utils/` | Shared utilities | Before creating new utility |

## Before Creating New Code

| Category | Check Location | Grep Command |
|----------|----------------|--------------|
| [Category] | `path/to/check/` | `rg "export function [pattern]"` |

## Quick Search Commands

Use these to find existing code before creating new implementations:

```bash
# Find existing [pattern type]
rg "export function [name]" [path]

# Find usages of [pattern]
rg "import.*from.*[module]" --type ts
```

## Do NOT Explore

These directories are not relevant for this context:

- `node_modules/` - External dependencies
- `dist/` | `build/` | `.next/` - Build artifacts
- `coverage/` - Test coverage reports
